# Paddle Configuration
NEXT_PUBLIC_VENDORID=173591

# Discount Override (for staging/testing)
# Set to 'true' to allow manual discount override via URL params
# NEXT_PUBLIC_ALLOW_DISCOUNT_OVERRIDE=true

# Manual Override URL Parameters (works in development or with NEXT_PUBLIC_ALLOW_DISCOUNT_OVERRIDE=true):
# 
# To show discount pricing (normally requires mobile + Instagram):
#   ?discount=true
#   ?preview=discount
#
# Default behavior (no params) = regular pricing
#
# Examples:
#   http://localhost:3000/                        # Regular pricing (default)
#   http://localhost:3000/?discount=true          # Show discount in dev
#   http://localhost:3000/?source=instagram       # Regular pricing (needs mobile too)
#   http://staging.site.com/?preview=discount     # Show discount on staging (if env var set)